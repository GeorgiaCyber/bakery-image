images:
  tags:
    - docker
  image: gacybercenter/bakery-image:latest
  script: |
      ### quick test - make real format later
      virt-builder centos-8.0 --install cloud-init --format raw
      virt-builder debian-10 --install cloud-init,lxqt --format raw
  artifacts:
    untracked: true
    when: on_success
    expire_in: 24 hours