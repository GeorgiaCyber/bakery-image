---

image_name: centos-8.0

compressed: false

# conversion: true

size: 4G

output_format: raw

compression:

method: virt-builder

packages:
    - ipcalc

customization: |
    #!/bin/bash
    echo "customization completed via virt-builder" > ./custom.txt
