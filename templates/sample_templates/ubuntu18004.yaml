---

image_name: ubuntu-18.04

compressed: false

conversion: true

output_format: raw

compression:

method: virt-builder

packages:
    - nmap

customization: |
    #!/bin/bash
    echo "customization completed via virt-builder" > ./custom.txt
