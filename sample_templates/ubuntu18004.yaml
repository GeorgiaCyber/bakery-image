---

image_name: ubuntu-18.04

compressed: false

convert: true

output_format: raw

compression:

method: virt-builder

packages:

customization: |
    #!/bin/bash
    echo "customization completed via virt-builder" > ./custom.txt
