---

image_name: centos-8.0

compressed: false

convert: true

output_format: raw

compression: gz

method: virt-builder

packages:
    - tcpdump
    - nmap

customization: |
    #!/bin/bash
    echo "customization completed via virt-builder" > ./custom.txt
